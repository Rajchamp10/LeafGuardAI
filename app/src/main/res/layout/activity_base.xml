<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/baseDrawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0F172A">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/baseToolbar"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:background="#1E293B"
            app:title="LeafGuard AI"
            app:titleTextColor="#FFFFFF" />

        <FrameLayout
            android:id="@+id/baseContentFrame"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />

    </LinearLayout>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="300dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="#1E293B"
        android:padding="24dp">

        <TextView
            android:id="@+id/txtMenuTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="MENU"
            android:textColor="@color/accent_orange"
            android:textSize="18sp"
            android:textStyle="bold"
            android:letterSpacing="0.1"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"/>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_marginTop="40dp"
            app:layout_constraintTop_toBottomOf="@id/txtMenuTitle"
            app:layout_constraintStart_toStartOf="parent">

            <TextView
                android:id="@+id/navScanDetect"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="📸  Scan &amp; Detect"
                android:textColor="@color/white"
                android:textSize="18sp"
                android:textStyle="bold"
                android:paddingVertical="16dp"/>

            <TextView
                android:id="@+id/navCostCalculator"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="💰  Cost Calculator"
                android:textColor="#BBBBBB"
                android:textSize="18sp"
                android:paddingVertical="16dp"/>

            <TextView
                android:id="@+id/navCalendar"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="📅  Calendar"
                android:textColor="#BBBBBB"
                android:textSize="18sp"
                android:paddingVertical="16dp"/>

            <TextView
                android:id="@+id/navHistory"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="🕒  History"
                android:textColor="#BBBBBB"
                android:textSize="18sp"
                android:paddingVertical="16dp"/>
        </LinearLayout>

        <com.ncorti.slidetoact.SlideToActView
            android:id="@+id/btnSwipeLogout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:text="Swipe to Logout"
            app:text_style="bold"
            app:outer_color="#FF3B30"
            app:inner_color="#FFFFFF"
            app:border_radius="12dp"
            app:slider_height="60dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.drawerlayout.widget.DrawerLayout>